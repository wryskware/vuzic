{
  "_comment": "The publish allowlist. `npm run build:publish` in web/ ships ONLY these tracks; every other timeline in data/timelines/ stays local. An allowlist, not a blacklist, so a track under test can never leak by being forgotten. The fallback track (`synthetic`, no audio) always ships — main.ts falls back to it and a build without it has a dead default path.",
  "tracks": [
    "bug-fix-rush",
    "drifting-slow-remastered",
    "free-fall",
    "lost-in-space-1-3",
    "pink-loop"
  ]
}
